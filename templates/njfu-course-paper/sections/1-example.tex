% !TEX root = ../main.tex
%
% 写作提示：
%   1. 保留 \section 和 \subsection，替换里面的示例文字即可
%   2. 不需要的图、表、公式、代码块可以直接删除
%   3. 参考文献条目写在 reference.bib，正文用 \cite{文献key} 引用
%

\section{引言}

这里写课程论文的研究背景、问题来源和写作目的。可以先用一两段说明为什么这个主题值得讨论，再引用相关文献作为支撑\cite{sample2024}。

本文围绕“你的论文主题”展开，主要分析以下问题：第一，说明研究对象的基本情况；第二，整理已有资料或课程知识；第三，结合案例、数据或计算结果给出自己的分析。

\section{研究对象与方法}

\subsection{研究对象}

这里介绍论文讨论的对象、案例、实验材料或课程设计任务。写作时尽量交代清楚时间、地点、数据来源和基本范围。

\subsection{分析方法}

这里说明你使用的分析方法。若论文中需要公式，可以使用 \autoref{eq:sample-model} 这样的编号引用。

\begin{equation}
    S = \frac{x_1 + x_2 + x_3}{3}
    \label{eq:sample-model}
\end{equation}

式中，$S$ 表示综合得分，$x_1$、$x_2$、$x_3$ 分别表示三个评价指标。请根据自己的课程内容替换变量含义。

\section{结果与分析}

\subsection{主要结果}

结果部分可以先用文字概括，再放表格或图片。表格建议使用三线表，如 \autoref{tab:sample-result}。

\begin{table}[htbp]
    \centering
    \caption{示例评价结果}
    \label{tab:sample-result}
    \begin{tabularx}{0.8\textwidth}{lCCC}
        \toprule
        对象 & 指标一 & 指标二 & 综合得分 \\
        \midrule
        样本 A & 82.5 & 78.0 & 80.3 \\
        样本 B & 91.0 & 85.5 & 88.2 \\
        样本 C & 76.5 & 80.0 & 78.3 \\
        \bottomrule
    \end{tabularx}
\end{table}

如果需要插入图片，把图片文件放进 \verb|figures/| 文件夹，然后取消下面代码中的注释并修改文件名。

\begin{figure}[htbp]
    \centering
    % \includegraphics[width=0.7\textwidth]{figures/your-image}
    \caption{示例图片标题}
    \label{fig:sample-image}
\end{figure}

\subsection{过程说明}

如果课程论文需要展示计算过程、实验步骤或程序片段，可以使用代码块。代码不是必需内容，不需要时删掉即可。

\begin{lstlisting}[language=Python, caption=示例代码片段]
scores = [82.5, 91.0, 76.5]
average = sum(scores) / len(scores)
print(average)
\end{lstlisting}

\section{讨论}

这里写你对结果的解释。可以讨论结果是否符合预期，和课程理论或参考文献有什么关系，还可以说明数据、方法或案例的局限。

写讨论时不要只重复表格数字，更重要的是解释“为什么会这样”和“这个结果说明了什么”。

\section{结论}

结论部分建议用一段或几条短句总结全文。可以回答引言中提出的问题，并说明本论文的主要发现。

例如：本文基于某课程主题完成了资料整理、结果分析和讨论，得到以下认识：第一，……；第二，……；第三，……。
